# Build, Test and Source Map

C++20 is required. `src/` contains the Arduino/bounded adapter, `docs/` describes its conversion contract, `tests/` validate failure preservation and representability, and `demos/` show supported Arduino integration.
