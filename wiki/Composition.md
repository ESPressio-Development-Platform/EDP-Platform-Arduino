# Composition

This repository does not introduce a new runtime Composition domain. It supplies external-type integration around EDP-Platform/BoundedTypes contracts. Ownership follows the Arduino API being adapted.
