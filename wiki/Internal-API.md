# Internal API

Internal helpers validate source lengths, capacity, embedded-NUL constraints and Arduino target publication. They exist to preserve strong failure semantics across the external Arduino type boundary.
